# Config para correr el backend FUERA de Docker (uvicorn local contra el Postgres del compose)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=zonaxtrema
POSTGRES_PASSWORD=zonaxtrema
POSTGRES_DB=zonaxtrema

SECRET_KEY=cambiar-en-produccion
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:5173

FACTPRO_BASE_URL=https://api.factpro.la/api/v3
FACTPRO_TOKEN=

# --- Emisor (debe coincidir con la cuenta duena del FACTPRO_TOKEN, que es
# quien firma). Si no se define, se usan los valores por defecto de config.py.
# Demo de FactPro: EMISOR_RUC=20600340647 / CONTRATISTAS PCKERNEL S.A.C.
EMISOR_RUC=
EMISOR_RAZON_SOCIAL=
