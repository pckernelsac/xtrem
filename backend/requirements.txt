fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
httpx==0.28.1
email-validator==2.2.0
weasyprint==63.1
jinja2==3.1.5
qrcode==8.0
pillow==11.1.0
openpyxl==3.1.5

# Emisión de comprobantes electrónicos a SUNAT sin intermediarios.
# Se fija por tag y no por rama: un deploy no debe cambiar de versión de
# facturación sin que nadie lo haya decidido.
# Va por el tarball del tag y no por `git+https`: la imagen no lleva git, y
# añadirlo sólo para esto engordaría el contenedor sin ganar nada.
sunat-cpe @ https://github.com/pckernelsac/pythoncpe/archive/refs/tags/v0.1.0.tar.gz
